{
  "mcpServers": {
    "ahq-mcp-server": {
      "command": "uv",
      "args": ["run", "--project", "${CLAUDE_PLUGIN_ROOT}", "python", "-m", "src.mcp_server"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "AHQ_MCP_HOME": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
